import React, { Components } from "react";

class Sidebar extends Components {
    render() {
        return (
            <div>

            </div>
        );
    }
}

export default Sidebar;